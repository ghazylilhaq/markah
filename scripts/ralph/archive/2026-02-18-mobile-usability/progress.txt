# Ralph Progress Log
Started: Tue Feb 18 2026
Feature: X (Twitter) Bookmarks Sync
---

## Codebase Patterns
- (inherited from previous run) Folder context menu: use `opacity-100 md:opacity-0 md:group-hover:opacity-100` for mobile-visible, desktop hover-reveal
- Sidebar component accepts optional `onNavigate` callback prop for Sheet close on navigation
- Use `useSyncExternalStore` for localStorage-backed state or media queries (no setState in useEffect)
- Touch targets: `min-h-[44px] min-w-[44px]` with flex centering, reset on desktop with `md:min-h-0 md:min-w-0`
- Tag color utility at `lib/utils/tag-color.ts` — `tagToColor()` for text, `tagBadgeStyle()` for full badge styles
- Responsive padding: `p-3 md:p-6` pattern for mobile-first
- `useMediaQuery` hook at `lib/hooks/use-media-query.ts` — uses `useSyncExternalStore`, returns false on server
- Sticky bar in padded container: use negative margins `-mx-3 px-3 md:-mx-6 md:px-6`
- Full-screen mobile dialog: override DialogContent positioning with responsive Tailwind classes
- .env.example is gitignored by .env* — use `git add -f .env.example` to commit
---
